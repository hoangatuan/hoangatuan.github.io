---
import PageLayout from "../../layouts/PageLayout.astro"
import BookList from "../../components/books/BookList.astro"
import { fetchBooks } from "../../utils/book";

const getBooks = async () => {
  const books = await fetchBooks();
  return books;
};

const books = await getBooks();
---

<PageLayout title="Books" description="Software engineering book reviews and reading notes.">
    <BookList prefix="books" books={books} />
</PageLayout>